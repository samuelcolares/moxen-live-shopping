export type Product = {
  name: string;
  productQty: string;
  images: string;
};

export type Live = {
  title: string;
  description: string;
  thumbnailUrl: string;
  dateStart: Date;
  dateEnd: Date;
  products: string;
}